import numpy as np

def moving_avg(ls, win_sz):
    count_moving_elements = len(arr) - window_sz + 1
    out = []
    for i in range(count_moving_elements):
        out.append(sum(arr[i:i+window_sz])/window_sz)
    return(out)
   
lst = [3,5,7,2,8,10,11,65,72,81,99,100,150]
arr = np.array(lst)

window_sz = 3

mv_avg = moving_avg(lst,window_sz)

print(mv_avg)
